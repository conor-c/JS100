let bigNumber = Number.MAX_SAFE_INTEGER;
console.log(bigNumber);
return bigNumber;

//the correct answer was Number.MAX_VALUE;
//Number.MAX_SAFE_INTEGER is just the largest number that can be used safely and with precision