let today = new Date();

console.log(today.getYear()); //returns 2-3 digit years given the date according to local time (DEPRECATED)
console.log(today.getFullYear()); //this returns 4 digits for 4 digit years of the given date according to local time